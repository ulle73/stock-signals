
added 43 packages, and audited 44 packages in 8s

8 packages are looking for funding
  run `npm fund` for details

2 vulnerabilities (1 moderate, 1 high)

To address all issues, run:
  npm audit fix

Run `npm audit` for details.
===== focused tests =====
TAP version 13
# Subtest: only Gamma Flip belongs to the default GEX/DEX chart group
ok 1 - only Gamma Flip belongs to the default GEX/DEX chart group
  ---
  duration_ms: 1.263065
  type: 'test'
  ...
# Subtest: strike selection keeps at most thirty rows on each side of spot and preserves distant key levels
ok 2 - strike selection keeps at most thirty rows on each side of spot and preserves distant key levels
  ---
  duration_ms: 2.983855
  type: 'test'
  ...
# Subtest: all provider strikes are shown when each side stays within the thirty-row cap
ok 3 - all provider strikes are shown when each side stays within the thirty-row cap
  ---
  duration_ms: 0.300549
  type: 'test'
  ...
# Subtest: options positioning model builds centered GEX and DEX bars with key-level annotations
ok 4 - options positioning model builds centered GEX and DEX bars with key-level annotations
  ---
  duration_ms: 0.921516
  type: 'test'
  ...
# Subtest: key-level history remains available for hover and keyboard tooltips
ok 5 - key-level history remains available for hover and keyboard tooltips
  ---
  duration_ms: 8.204881
  type: 'test'
  ...
# Subtest: chart renders isolated Options Positioning strike bars instead of the old ladder table
not ok 6 - chart renders isolated Options Positioning strike bars instead of the old ladder table
  ---
  duration_ms: 10.89007
  type: 'test'
  location: '/home/runner/work/stock-signals/stock-signals/tests/options-ladder.test.js:115:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /from gex_dex_strike_snapshots/. Input:
    
    "import { query } from '../db.js';\n" +
      '\n' +
      'function finiteNumber(value) {\n' +
      "  if (value === null || value === undefined || value === '') return null;\n" +
      '  const number = Number(value);\n' +
      '  return Number.isFinite(number) ? number : null;\n' +
      '}\n' +
      '\n' +
      'export async function getLatestGexDexStrikeSnapshot(ticker) {\n' +
      '  const result = await query(\n' +
      '    `with latest as (\n' +
      '       select\n' +
      '         id,\n' +
      '         source_timestamp,\n' +
      '         spot_price\n' +
      '       from gex_dex_source_snapshots\n' +
      '       where ticker = $1\n' +
      '         and source_timestamp <= now()\n' +
      '       order by source_timestamp desc\n' +
      '       limit 1\n' +
      '     )\n' +
      '     select\n' +
      '       latest.source_timestamp::text as source_timestamp,\n' +
      '       latest.spot_price::text as spot_price,\n' +
      '       strike.strike::text as strike,\n' +
      '       strike.call_gex::text as call_gex,\n' +
      '       strike.put_gex::text as put_gex,\n' +
      '       strike.net_gex::text as net_gex,\n' +
      '       strike.call_dex::text as call_dex,\n' +
      '       strike.put_dex::text as put_dex,\n' +
      '       strike.net_dex::text as net_dex,\n' +
      '       strike.expiry_count::text as expiry_count\n' +
      '     from latest\n' +
      '     left join gex_dex_strike_snapshots strike\n' +
      '       on strike.snapshot_id = latest.id\n' +
      '     order by strike.strike asc`,\n' +
      '    [ticker]\n' +
      '  );\n' +
      '\n' +
      '  const first = result.rows[0] ?? null;\n' +
      '  if (!first) {\n' +
      '    return { sourceTimestamp: null, spotPrice: null, strikes: [] };\n' +
      '  }\n' +
      '\n' +
      '  return {\n' +
      '    sourceTimestamp: first.source_timestamp ?? null,\n' +
      '    spotPrice: finiteNumber(first.spot_price),\n' +
      '    strikes: result.rows.flatMap((row) => {\n' +
      '      const strike = finiteNumber(row.strike);\n' +
      '      if (strike === null) return [];\n' +
      '      return [{\n' +
      '        strike,\n' +
      '        call_gex: finiteNumber(row.call_gex),\n' +
      '        put_gex: finiteNumber(row.put_gex),\n' +
      '        net_gex: finiteNumber(row.net_gex),\n' +
      '        call_dex: finiteNumber(row.call_dex),\n' +
      '        put_dex: finiteNumber(row.put_dex),\n' +
      '        net_dex: finiteNumber(row.net_dex),\n' +
      '        expiry_count: finiteNumber(row.expiry_count),\n' +
      '      }];\n' +
      '    }),\n' +
      '  };\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import { query } from '../db.js';
    
    function finiteNumber(value) {
      if (value === null || value === undefined || value === '') return null;
      const number = Number(value);
      return Number.isFinite(number) ? number : null;
    }
    
    export async function getLatestGexDexStrikeSnapshot(ticker) {
      const result = await query(
        `with latest as (
           select
             id,
             source_timestamp,
             spot_price
           from gex_dex_source_snapshots
           where ticker = $1
             and source_timestamp <= now()
           order by source_timestamp desc
           limit 1
         )
         select
           latest.source_timestamp::text as source_timestamp,
           latest.spot_price::text as spot_price,
           strike.strike::text as strike,
           strike.call_gex::text as call_gex,
           strike.put_gex::text as put_gex,
           strike.net_gex::text as net_gex,
           strike.call_dex::text as call_dex,
           strike.put_dex::text as put_dex,
           strike.net_dex::text as net_dex,
           strike.expiry_count::text as expiry_count
         from latest
         left join gex_dex_strike_snapshots strike
           on strike.snapshot_id = latest.id
         order by strike.strike asc`,
        [ticker]
      );
    
      const first = result.rows[0] ?? null;
      if (!first) {
        return { sourceTimestamp: null, spotPrice: null, strikes: [] };
      }
    
      return {
        sourceTimestamp: first.source_timestamp ?? null,
        spotPrice: finiteNumber(first.spot_price),
        strikes: result.rows.flatMap((row) => {
          const strike = finiteNumber(row.strike);
          if (strike === null) return [];
          return [{
            strike,
            call_gex: finiteNumber(row.call_gex),
            put_gex: finiteNumber(row.put_gex),
            net_gex: finiteNumber(row.net_gex),
            call_dex: finiteNumber(row.call_dex),
            put_dex: finiteNumber(row.put_dex),
            net_dex: finiteNumber(row.net_dex),
            expiry_count: finiteNumber(row.expiry_count),
          }];
        }),
      };
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/stock-signals/stock-signals/tests/options-ladder.test.js:134:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
1..6
# tests 6
# suites 0
# pass 5
# fail 1
# cancelled 0
# skipped 0
# todo 0
# duration_ms 83.598932
